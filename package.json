{
  "private": true,
  "scripts": {
    "gulp": "cd gulp && npx gulp",
    "sass:school": "cd gulp && npx gulp stylesSchool",
    "sass:main": "cd gulp && npx gulp styles",
    "build:css": "cd gulp && npx gulp styles stylesSchool"
  },
  "devDependencies": {
    "scroll-hint": "^1.2.9"
  }
}
